import * as React from 'react';
import { Size, Viewport } from './config';
declare type Direction = 'row' | 'col';
declare type DirectionViewport = {
    [key in Viewport]?: Direction;
};
declare type VerticalAlignment = 'top' | 'center' | 'bottom' | 'justify';
declare type VerticalAlignmentViewport = {
    [key in Viewport]?: VerticalAlignment;
};
declare type HorizontalAlignment = 'left' | 'right' | 'center' | 'stretch';
declare type HorizontalAlignmentViewport = {
    [key in Viewport]?: HorizontalAlignment;
};
declare type Order = 'first' | 'last';
export interface ColumnProps {
    children?: any;
    size?: Size | {
        [key in Viewport]?: Size;
    };
    direction?: Direction | DirectionViewport;
    verticalAlignment?: VerticalAlignment | VerticalAlignmentViewport;
    horizontalAlignment?: HorizontalAlignment | HorizontalAlignmentViewport;
    offset?: Size | {
        [key in Viewport]?: Size;
    };
    order?: Size | Order | {
        [key in Viewport]?: Size | Order;
    };
    className?: string;
}
export declare class Column extends React.PureComponent<ColumnProps> {
    render(): JSX.Element;
}
export {};
